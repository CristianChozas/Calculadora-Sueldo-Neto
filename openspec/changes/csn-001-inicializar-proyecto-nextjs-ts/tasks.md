# Tasks

1. Bootstrap Next.js with TypeScript, Tailwind CSS and ESLint.
2. Adapt the generated scaffold to the repository root layout.
3. Create `components/` and `lib/` directories for upcoming tickets.
4. Verify the scaffold with lint and build.
5. Commit the ticket with the exact roadmap title.
