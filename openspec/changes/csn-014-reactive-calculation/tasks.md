# Tasks

1. Add the missing age input required by the fiscal model.
2. Build a valid `FormData` object from the current controlled inputs.
3. Recalculate the salary estimate automatically on every relevant change.
4. Show a waiting state while validation errors exist.
5. Verify lint and build.
6. Commit the ticket with the exact roadmap title.
