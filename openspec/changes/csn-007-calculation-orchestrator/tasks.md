# Tasks

1. Compose the IRPF and social security modules from `FormData`.
2. Derive taxable base, annual net, and monthly net values.
3. Return a `CalculationResult` contract ready for presentation.
4. Verify lint and build.
5. Commit the ticket with the exact roadmap title.
